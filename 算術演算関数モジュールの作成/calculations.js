"use strict";
//calculations.tsファイルから関数をエクスポートする文
let x = 0;
let y = 0;
//加算関数（add）
const getRectang = function (x, y) {
    return x + y;
};
console.log(getRectang(3, 5));
//減算関数（subtract）
//乗算関数（multiply）
//除算関数（divide）
